default:
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
          sessions:
            default:
              symfony2: ~
    suites:
        default:
            contexts:
                - Test\Integration\Context\ApiContext:
                    doctrine: @doctrine
                # - Test\Integration\Context\CreateContext:
                #     container: @service_container
        # get:
        #     contexts:
        #         - Test\Integration\Context\FeatureContext
        #     filters:
        #         tags: '@get'
