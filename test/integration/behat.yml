default:
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
          sessions:
            default:
              symfony2: ~
    suites:
        default:
            contexts:
                - Test\Integration\Context\FeatureContext:
                    doctrine: @doctrine
        # get:
        #     contexts:
        #         - Test\Integration\Context\FeatureContext
        #     filters:
        #         tags: '@get'
