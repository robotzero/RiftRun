parameters:
    minParagon: <numberBetween($min = 70, $max = 1000)>
    createdAt: <dateTime($max = 'now')>

RiftRunBundle\Model\SearchQuery:
    searchquery (template):
        __construct: ['<numberBetween(70, 1000)>', '@grifts<current()>', '<dateTime("now")>']
